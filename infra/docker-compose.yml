services:
  dozzle:
    image: amir20/dozzle:latest
    container_name: devstack-dozzle
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    ports:
      - "9999:8080"
    restart: unless-stopped
